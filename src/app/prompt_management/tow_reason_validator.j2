You are a strict validator.

Determine whether the following user input is a valid reason for requesting a vehicle tow.

User input:
"{{ user_input }}"

Respond ONLY in valid JSON with no extra text.

Format:
{
  "is_valid": true | false
}
